# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Myanmar Lens' do
  
  use_frameworks!
  
  pod 'SwiftyTesseract'
#  pod 'TesseractOCRiOS'
  target 'Myanmar LensTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'Myanmar LensUITests' do
    inherit! :search_paths
    # Pods for testing
  end
  
end
